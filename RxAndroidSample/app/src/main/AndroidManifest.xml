<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.passionvirus.rxandroidsample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.TimerTaskActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.LoopActivity" />
        <activity android:name=".ui.EventListenerActivity" />
        <activity android:name=".ui.RecyclerViewActivity" />
        <activity android:name=".ui.AsyncTaskActivity" />
        <activity android:name=".ui.TimerTaskActivity" />
        <activity android:name=".ui.RetrofitActivity" />
        <activity android:name=".ui.DebuggingActivity" />
        <activity android:name=".ui.ExceptionActivity" />
        <activity android:name=".ui.SampleActivity" />
        <activity android:name=".ui.BufferActivity" />
        <activity android:name=".ui.ThrottleFirstActivity" />
        <activity android:name=".ui.WindowActivity" />
        -->
    </application>

</manifest>